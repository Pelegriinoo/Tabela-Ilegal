name: Build and Deploy
on:
  push:
    branches: [ main ]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Inject Discord Webhook 🔑
        run: |
          # Substitui o placeholder pelo webhook real
          sed -i "s|DISCORD_WEBHOOK_URL_PLACEHOLDER|${{ secrets.DISCORD_WEBHOOK_URL }}|g" discord.js
        
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: .
